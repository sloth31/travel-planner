// 文件: next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // 你的 Next.js 配置会在这里
  // 默认的 create-next-app 可能是
  // reactStrictMode: true,

  // ...
};

export default nextConfig;